{
  "groups": [
    {
      "mask": "[a-z]+.log",
      "rules": [
        {
          "name": "keys_count",
          "points_regexp": "metric dbcount ([0-9]+)$",
          "types": [
            "int"
          ],
          "columns": [
            "count"
          ]
        }
      ]
    }
  ]

}