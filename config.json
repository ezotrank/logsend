{
  "groups": [
    {
      "mask": "[a-z]+.log",
      "rules": [
        {
          "name": "keys_count",
          "regexp": "metric dbcount ([0-9]+)$",
          "columns": [
            ["count", "int"]
          ]
        },
        {
          "name": "prepare_proposals",
          "regexp": "PreapreProposals took (.+)$",
          "columns": [
            ["duration", "int", "DurationToMillisecond"]
          ]
        }
      ]
    }
  ]

}